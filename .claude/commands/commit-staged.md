---
name: "Commit Staged"
description: Commit exactly what's currently staged in git
category: Workflow
tags: [git, commit]
---

Use the `commit-staged-changes` skill to commit exactly what's currently staged in git. Do not stage additional files unless the user explicitly asks.
